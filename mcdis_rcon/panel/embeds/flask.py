from ...modules import *
from ...classes import *
from ...utils import *

def flask_embed(client: McDisClient) -> discord.Embed:

    embed = discord.Embed(
        title = f'> Flask',
        colour = embed_colour,
        description = 
        client._('asd.'))\
    .set_footer(text = 185*blank_space)
  
    return embed